# 移动端 H5 组件库定制方案

**项目类型：** 移动端 H5 应用
**目标：** 基于成熟的移动端组件库进行主题定制，统一颜色、样式、交互

**创建日期：** 2025-12-07

---

## 📊 整体方案

```
第一步              第二步              第三步              第四步
设计规范梳理    →   选择移动端组件库  →  主题配置        →   应用到项目
(3-4天)            (1天)              (2-3天)            (持续)
```

---

## 🎯 第一步：移动端设计规范梳理（3-4 天）

**重点：** 移动端和 PC 端的设计规范有很大区别！

### 移动端特殊考虑

| 设计要素     | PC 端         | 移动端                                | 说明                                      |
| ------------ | ------------- | ------------------------------------- | ----------------------------------------- |
| **设计宽度** | 1200px-1920px | 375px (iPhone) / 750px (设计稿)       | 移动端通常以 iPhone 6/7/8 的 375px 为基准 |
| **字号**     | 14px-16px     | 28px-32px (设计稿) / 14px-16px (实际) | 设计稿通常是 2 倍图                       |
| **按钮高度** | 32px-40px     | 88px (设计稿) / 44px (实际)           | 需考虑手指点击区域                        |
| **间距**     | 8px/16px/24px | 16px/32px/48px (设计稿)               | 设计稿 2 倍尺寸                           |
| **交互**     | 鼠标悬停      | 触摸点击、滑动、长按                  | 移动端交互方式完全不同                    |

---

### 需要梳理的设计要素

#### 1. 颜色系统（与 PC 端相同）

| 设计要素   | 需要提取的内容       | 对应移动端组件库配置                          |
| ---------- | -------------------- | --------------------------------------------- |
| **主色**   | 品牌色               | `--adm-color-primary` / `--van-primary-color` |
| **成功色** | 成功状态             | `--adm-color-success` / `--van-success-color` |
| **警告色** | 警告提示             | `--adm-color-warning` / `--van-warning-color` |
| **错误色** | 错误提示             | `--adm-color-danger` / `--van-danger-color`   |
| **文字色** | 标题、正文、辅助文字 | `--adm-color-text`                            |
| **背景色** | 页面背景、卡片背景   | `--adm-color-background`                      |
| **边框色** | 分割线               | `--adm-color-border`                          |

**梳理方法：**

- [ ] 从历史移动端项目（如果有）中提取
- [ ] 或者从 PC 端项目的颜色系统继承
- [ ] 使用开发者工具（Chrome DevTools 移动端模式）查看

---

#### 2. 字体系统（移动端特殊）

**重要：** 移动端字号需要考虑：

- 最小可读字号：12px（实际）
- 常规正文：14px-16px（实际）
- 设计稿通常是 2 倍尺寸（28px-32px）

| 用途       | 设计稿字号 (2x) | 实际字号 (1x) | CSS 配置            |
| ---------- | --------------- | ------------- | ------------------- |
| 页面大标题 | 36px            | 18px          | `--adm-font-size-9` |
| 模块标题   | 32px            | 16px          | `--adm-font-size-8` |
| 卡片标题   | 28px            | 14px          | `--adm-font-size-7` |
| 正文       | 28px            | 14px          | `--adm-font-size-5` |
| 辅助文字   | 24px            | 12px          | `--adm-font-size-3` |

**梳理清单：**

- [ ] 页面标题字号
- [ ] 卡片/列表标题字号
- [ ] 正文字号
- [ ] 辅助信息字号（时间、标签等）
- [ ] 字重（400/500/600）

---

#### 3. 间距系统（移动端特殊）

**重要：** 移动端点击区域不能太小！

| 设计要素         | 最小尺寸要求 | 推荐尺寸  |
| ---------------- | ------------ | --------- |
| **按钮高度**     | 44px         | 44px-48px |
| **点击区域**     | 44x44px      | 48x48px   |
| **列表项高度**   | 44px         | 50px-60px |
| **底部导航高度** | 50px         | 50px-60px |

**间距梳理：**

| 名称 | 设计稿 (2x) | 实际 (1x) | 使用场景             |
| ---- | ----------- | --------- | -------------------- |
| xs   | 8px         | 4px       | 图标与文字间距       |
| sm   | 16px        | 8px       | 小间距               |
| md   | 32px        | 16px      | 卡片内边距、常规间距 |
| lg   | 48px        | 24px      | 模块间距             |
| xl   | 64px        | 32px      | 大区块间距           |

**梳理清单：**

- [ ] 页面左右边距（通常 16px-20px）
- [ ] 卡片内边距
- [ ] 列表项内边距
- [ ] 按钮高度
- [ ] 底部导航高度
- [ ] 顶部导航栏高度（通常 44px-48px）

---

#### 4. 圆角规范

移动端圆角通常比 PC 端大一些，更圆润。

| 组件     | 推荐圆角  | 使用场景           |
| -------- | --------- | ------------------ |
| 小圆角   | 4px       | 标签、徽章         |
| 中等圆角 | 8px       | 按钮、输入框       |
| 大圆角   | 12px-16px | 卡片、弹窗         |
| 超大圆角 | 20px-24px | 底部抽屉、特殊卡片 |

---

#### 5. 移动端特有交互

**需要统一的交互：**

- [ ] **点击反馈** - 按钮/列表项点击时的效果（通常是背景色变化）
- [ ] **下拉刷新** - 样式和交互
- [ ] **上拉加载** - 样式和交互
- [ ] **滑动删除** - 列表项滑动交互
- [ ] **弹出层** - Popup、ActionSheet、Dialog 的动画
- [ ] **长按操作** - 长按触发菜单

---

#### 6. 移动端常用组件梳理

**从历史项目中重点关注：**

##### 📱 导航类

- [ ] **顶部导航栏（NavBar）**

  - 高度、背景色、标题文字大小
  - 返回按钮样式
  - 右侧操作按钮

- [ ] **底部标签栏（TabBar）**
  - 高度、背景色
  - 图标大小
  - 选中态颜色

##### 📱 列表类

- [ ] **列表（List）**

  - 列表项高度
  - 内边距
  - 分割线样式
  - 右侧箭头

- [ ] **卡片（Card）**
  - 圆角、阴影
  - 内边距
  - 间距

##### 📱 表单类

- [ ] **按钮（Button）**

  - 主按钮、次按钮样式
  - 高度（通常 44px-48px）
  - 圆角

- [ ] **输入框（Input）**

  - 高度
  - 边框样式
  - 聚焦样式

- [ ] **搜索框（SearchBar）**
  - 高度、圆角
  - 背景色
  - 图标样式

##### 📱 反馈类

- [ ] **Toast（轻提示）**

  - 背景色、圆角
  - 文字大小

- [ ] **Dialog（对话框）**

  - 圆角、阴影
  - 按钮样式

- [ ] **ActionSheet（动作面板）**
  - 样式、圆角

##### 📱 其他

- [ ] **标签（Tag）**
- [ ] **徽章（Badge）**
- [ ] **步骤条（Steps）**
- [ ] **轮播图（Swiper）**

---

## 🎯 第二步：选择移动端组件库（1 天）

### 移动端组件库对比

#### **Ant Design Mobile**（推荐 - React）

**官网：** <https://mobile.ant.design/zh>

**优点：**

- ✅ 蚂蚁集团出品，质量有保障
- ✅ 组件丰富，覆盖大部分场景
- ✅ 主题定制强大（CSS 变量）
- ✅ TypeScript 支持完善
- ✅ 文档详细，示例丰富

**主题配置示例：**

```tsx
import { ConfigProvider } from 'antd-mobile';

<ConfigProvider
  theme={{
    '--adm-color-primary': '#1677FF',
    '--adm-color-success': '#52c41a',
    '--adm-font-size-main': '14px',
  }}
>
  <App />
</ConfigProvider>;
```

**适合场景：**

- 中后台移动端应用
- 数据展示类 H5
- 复杂交互的移动应用

**推荐指数：** ⭐⭐⭐⭐⭐

---

#### **Vant**（推荐 - Vue）

**官网：** <https://vant-ui.github.io/vant/>

**优点：**

- ✅ 有赞团队出品，电商场景经验丰富
- ✅ 组件丰富，轻量高效
- ✅ 主题定制方便（Less 变量 + CSS 变量）
- ✅ 支持 Vue 2 和 Vue 3
- ✅ 文档清晰，中文友好

**主题配置示例：**

```vue
<template>
  <ConfigProvider :theme-vars="themeVars">
    <App />
  </ConfigProvider>
</template>

<script>
export default {
  data() {
    return {
      themeVars: {
        primaryColor: '#1677FF',
        successColor: '#52c41a',
        buttonBorderRadius: '8px',
      },
    };
  },
};
</script>
```

**适合场景：**

- 电商类 H5
- 营销活动页
- 常规移动端应用

**推荐指数：** ⭐⭐⭐⭐⭐

---

#### **NutUI**（备选 - Vue/React/Taro）

**官网：** <https://nutui.jd.com/>

**优点：**

- ✅ 京东出品
- ✅ 同时支持 Vue 和 React
- ✅ 支持多端（H5、小程序、APP）
- ✅ 组件设计现代

**适合场景：**

- 需要多端统一的项目（H5 + 小程序）
- 需要同时支持 Vue 和 React

**推荐指数：** ⭐⭐⭐⭐

---

#### **Ionic**（备选 - 多框架）

**官网：** <https://ionicframework.com/>

**优点：**

- ✅ 支持 React/Vue/Angular
- ✅ 原生应用级体验
- ✅ 组件丰富

**缺点：**

- ⚠️ 偏向混合 APP，纯 H5 可能过重

**推荐指数：** ⭐⭐⭐

---

### 选择建议

**根据技术栈选择：**

| 技术栈                | 首选              | 备选            |
| --------------------- | ----------------- | --------------- |
| **React**             | Ant Design Mobile | NutUI (React)   |
| **Vue 3**             | Vant 4            | NutUI (Vue 3)   |
| **Vue 2**             | Vant 2            | -               |
| **多端（H5+小程序）** | Taro + NutUI      | uni-app + uView |

**从你的项目看（GEBD HOPE、肠道大会、Rinstitute）：**

- 科研数据类、内容展示类 → **Ant Design Mobile** 或 **Vant**
- 简洁、专业的风格

**💡 我的推荐：**

- 如果用 **React** → **Ant Design Mobile**
- 如果用 **Vue** → **Vant**

---

## 🎯 第三步：主题配置（2-3 天）

### 方案 A：Ant Design Mobile 主题配置

#### 3.1 创建主题配置文件

```typescript
// src/theme/index.ts

export const mobileTheme = {
  // ===== 颜色系统 =====
  '--adm-color-primary': '#1677FF', // 主色（从历史项目提取）
  '--adm-color-success': '#52c41a', // 成功色
  '--adm-color-warning': '#faad14', // 警告色
  '--adm-color-danger': '#ff3141', // 错误色

  '--adm-color-text': '#333333', // 主文本
  '--adm-color-text-secondary': '#666666', // 次要文本
  '--adm-color-weak': '#999999', // 弱文本
  '--adm-color-light': '#cccccc', // 浅色文本

  '--adm-color-background': '#ffffff', // 背景色
  '--adm-color-border': '#eeeeee', // 边框色

  // ===== 字体系统 =====
  '--adm-font-size-main': '14px', // 主字号
  '--adm-font-size-1': '9px',
  '--adm-font-size-2': '10px',
  '--adm-font-size-3': '12px', // 辅助文字
  '--adm-font-size-4': '13px',
  '--adm-font-size-5': '14px', // 正文
  '--adm-font-size-6': '15px',
  '--adm-font-size-7': '16px', // 小标题
  '--adm-font-size-8': '18px', // 标题
  '--adm-font-size-9': '20px',
  '--adm-font-size-10': '24px', // 大标题

  '--adm-font-family':
    '-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif',

  // ===== 圆角 =====
  '--adm-radius-s': '4px', // 小圆角
  '--adm-radius-m': '8px', // 中圆角
  '--adm-radius-l': '12px', // 大圆角

  // ===== 边框 =====
  '--adm-border-width': '1px',

  // ===== 组件特定 =====
  // 按钮
  '--adm-button-border-radius': '8px', // 按钮圆角
  '--adm-button-text-font-size': '16px', // 按钮文字大小

  // 导航栏
  '--adm-nav-bar-height': '44px', // 导航栏高度

  // 列表
  '--adm-list-item-padding-left': '16px', // 列表项左边距
  '--adm-list-item-padding-right': '16px', // 列表项右边距

  // 卡片
  '--adm-card-border-radius': '12px', // 卡片圆角
  '--adm-card-padding': '16px', // 卡片内边距

  // 标签页
  '--adm-tab-bar-height': '50px', // 底部标签栏高度
};
```

#### 3.2 应用到项目

```tsx
// src/App.tsx
import { ConfigProvider } from 'antd-mobile';
import { mobileTheme } from './theme';

function App() {
  return (
    <ConfigProvider theme={mobileTheme}>{/* 你的应用内容 */}</ConfigProvider>
  );
}

export default App;
```

---

### 方案 B：Vant 主题配置

#### 3.1 创建主题配置文件

```typescript
// src/theme/index.ts

export const vantTheme = {
  // ===== 颜色系统 =====
  primaryColor: '#1677FF', // 主题色
  successColor: '#52c41a', // 成功色
  warningColor: '#faad14', // 警告色
  dangerColor: '#ff3141', // 危险色

  textColor: '#333333', // 文本色
  textColor2: '#666666', // 次要文本色
  textColor3: '#999999', // 三级文本色

  backgroundColor: '#f7f8fa', // 背景色
  backgroundColor2: '#ffffff', // 二级背景色

  borderColor: '#eeeeee', // 边框色

  // ===== 字体系统 =====
  fontSizeXs: '10px',
  fontSizeSm: '12px',
  fontSizeMd: '14px', // 正文
  fontSizeLg: '16px', // 标题
  fontSizeXl: '18px',

  fontWeightBold: '600',

  // ===== 圆角 =====
  borderRadiusSm: '4px',
  borderRadiusMd: '8px',
  borderRadiusLg: '12px',
  borderRadiusMax: '999px',

  // ===== 间距 =====
  paddingBase: '4px',
  paddingXs: '8px',
  paddingSm: '12px',
  paddingMd: '16px',
  paddingLg: '24px',
  paddingXl: '32px',

  // ===== 组件特定 =====
  // 按钮
  buttonBorderRadius: '8px',
  buttonDefaultHeight: '44px',
  buttonDefaultFontSize: '16px',

  // 导航栏
  navBarHeight: '44px',
  navBarBackgroundColor: '#ffffff',
  navBarTitleFontSize: '16px',

  // 标签页
  tabbarHeight: '50px',

  // 单元格（列表项）
  cellHorizontalPadding: '16px',
  cellVerticalPadding: '12px',
  cellFontSize: '14px',

  // 卡片
  cardPadding: '16px',
  cardBorderRadius: '12px',
};
```

#### 3.2 应用到项目

```vue
<!-- src/App.vue -->
<template>
  <ConfigProvider :theme-vars="vantTheme">
    <router-view />
  </ConfigProvider>
</template>

<script setup>
import { ConfigProvider } from 'vant';
import { vantTheme } from './theme';
</script>
```

---

### 3.3 多主题方案对比（可选）

如果还在纠结设计方案，可以创建多套主题：

```typescript
// src/theme/themes.ts

// 方案 A：科技蓝主题
export const theme1 = {
  '--adm-color-primary': '#1677FF',
  '--adm-radius-m': '8px',
  // ...
};

// 方案 B：医疗绿主题
export const theme2 = {
  '--adm-color-primary': '#13c2c2',
  '--adm-radius-m': '12px',
  // ...
};

// 方案 C：稳重灰主题
export const theme3 = {
  '--adm-color-primary': '#434343',
  '--adm-radius-m': '6px',
  // ...
};
```

**实现主题切换：**

```tsx
import { useState } from 'react';
import { ConfigProvider, Button, Space } from 'antd-mobile';
import { theme1, theme2, theme3 } from './theme/themes';

function App() {
  const [currentTheme, setCurrentTheme] = useState(theme1);

  return (
    <ConfigProvider theme={currentTheme}>
      {/* 主题切换按钮 */}
      <Space>
        <Button onClick={() => setCurrentTheme(theme1)}>方案 A</Button>
        <Button onClick={() => setCurrentTheme(theme2)}>方案 B</Button>
        <Button onClick={() => setCurrentTheme(theme3)}>方案 C</Button>
      </Space>

      {/* 你的应用内容 */}
    </ConfigProvider>
  );
}
```

直接在手机或浏览器移动端模式中对比效果！

---

### 3.4 自定义样式覆盖

**对于主题配置无法满足的特殊需求：**

```css
/* src/styles/custom.css */

/* 自定义顶部导航栏（参考历史项目） */
.custom-navbar {
  background: linear-gradient(90deg, #1677ff 0%, #0052d9 100%);
  height: 44px;
}

.custom-navbar .adm-nav-bar-title {
  color: #ffffff;
  font-size: 18px;
  font-weight: 600;
}

/* 自定义搜索框 */
.custom-search-bar {
  border-radius: 20px;
  background-color: #f5f5f5;
  padding: 8px 16px;
}

/* 自定义卡片 */
.custom-card {
  border-radius: 12px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
  margin-bottom: 12px;
}

/* 列表项点击效果 */
.adm-list-item:active {
  background-color: #f5f5f5;
}
```

---

## 🎯 第四步：应用到项目（持续）

### 4.1 项目结构建议

```
your-mobile-project/
├── src/
│   ├── theme/
│   │   ├── index.ts           # 主题配置
│   │   └── themes.ts          # 多主题方案（可选）
│   ├── styles/
│   │   └── custom.css         # 自定义样式
│   ├── components/
│   │   ├── CustomNavBar/      # 自定义导航栏
│   │   ├── CustomSearchBar/   # 自定义搜索框
│   │   └── CustomCard/        # 自定义卡片
│   ├── pages/
│   │   ├── Home/              # 首页
│   │   ├── List/              # 列表页
│   │   └── Detail/            # 详情页
│   ├── App.tsx
│   └── main.tsx
├── public/
└── package.json
```

### 4.2 移动端适配

**viewport 配置：**

```html
<!-- public/index.html -->
<meta
  name="viewport"
  content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
/>
```

**rem 适配方案（可选）：**

```typescript
// src/utils/flexible.ts
const setRem = () => {
  const baseSize = 37.5; // 基于 375px 设计稿
  const scale = document.documentElement.clientWidth / 375;
  document.documentElement.style.fontSize = baseSize * scale + 'px';
};

setRem();
window.addEventListener('resize', setRem);
```

**或使用 vw 方案：**

```css
/* postcss.config.js 配置 postcss-px-to-viewport */
module.exports = {
  plugins: {
    'postcss-px-to-viewport': {
      viewportWidth: 375,  // 设计稿宽度
      unitPrecision: 3,
      viewportUnit: 'vw',
      selectorBlackList: ['.ignore'],
      minPixelValue: 1,
    },
  },
};
```

---

## 📋 完整任务清单

### ✅ 阶段一：移动端设计规范梳理（3-4 天）

- [ ] **准备工作**

  - [ ] 在 Chrome 中开启移动端模拟（F12 → 设备工具栏）
  - [ ] 安装 ColorZilla 等工具

- [ ] **梳理历史项目（移动端或 PC 端）**

  - [ ] 截图关键页面（记得用移动端视口）
  - [ ] 提取颜色值
  - [ ] 测量字体（注意是否是 2 倍图）
  - [ ] 测量间距（按钮高度、列表项高度、页面边距）
  - [ ] 测量圆角
  - [ ] 记录交互方式（点击反馈、滑动等）

- [ ] **重点关注移动端组件**

  - [ ] 顶部导航栏（高度、样式）
  - [ ] 底部标签栏（高度、图标、样式）
  - [ ] 搜索框（样式、尺寸）
  - [ ] 列表/卡片（高度、内边距、间距）
  - [ ] 按钮（高度、圆角）

- [ ] **填充《设计规范梳理文档.md》**
  - [ ] 标注设计稿是否为 2 倍图
  - [ ] 记录实际 CSS 值

### ✅ 阶段二：选择移动端组件库（1 天）

- [ ] **确认技术栈**

  - [ ] React / Vue 3 / Vue 2 / 其他？
  - [ ] 是否需要多端（H5 + 小程序）？

- [ ] **组件库选型**

  - [ ] React → Ant Design Mobile
  - [ ] Vue → Vant
  - [ ] 多端 → Taro + NutUI

- [ ] **安装组件库**

  ```bash
  # Ant Design Mobile
  npm install antd-mobile

  # 或 Vant
  npm install vant
  ```

### ✅ 阶段三：主题配置（2-3 天）

- [ ] **创建主题配置文件**

  - [ ] 根据梳理的设计规范创建 `theme/index.ts`
  - [ ] 配置颜色、字体、间距、圆角

- [ ] **应用到项目**

  - [ ] 在 `App.tsx` 中应用 `ConfigProvider`
  - [ ] 测试基础组件（Button、List、Card、NavBar）

- [ ] **多主题方案（可选）**

  - [ ] 创建 2-3 套主题
  - [ ] 实现切换功能
  - [ ] 在手机上对比效果

- [ ] **样式覆盖**
  - [ ] 创建 `styles/custom.css`
  - [ ] 覆盖特殊样式
  - [ ] 封装自定义组件

### ✅ 阶段四：应用到项目（持续）

- [ ] **移动端适配**

  - [ ] 配置 viewport
  - [ ] 选择适配方案（rem / vw）

- [ ] **开发示例页面**

  - [ ] 首页
  - [ ] 列表页
  - [ ] 详情页

- [ ] **真机测试**
  - [ ] iOS Safari
  - [ ] Android Chrome
  - [ ] 微信浏览器

---

## 📱 移动端特殊注意事项

### 1. 安全区域（iPhone X 及以上）

```css
/* 适配刘海屏 */
body {
  padding-top: constant(safe-area-inset-top);
  padding-top: env(safe-area-inset-top);
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}
```

### 2. 1px 边框问题

```css
/* 移动端 1px 边框可能显示过粗 */
.border-1px {
  position: relative;
}

.border-1px::after {
  content: '';
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 1px;
  background-color: #e5e5e5;
  transform: scaleY(0.5);
}
```

### 3. 点击延迟

```javascript
// 移除 300ms 点击延迟
import FastClick from 'fastclick';
FastClick.attach(document.body);
```

或使用 CSS：

```css
* {
  touch-action: manipulation;
}
```

### 4. 滚动优化

```css
/* iOS 滚动流畅 */
.scroll-container {
  -webkit-overflow-scrolling: touch;
  overflow-y: scroll;
}
```

---

## 🛠 推荐工具

### 设计规范梳理

- **Chrome DevTools** - F12 → 设备工具栏（移动端模拟）
- **ColorZilla** - 颜色提取
- **Responsively App** - 多设备同时预览

### 移动端调试

- **vConsole** - 移动端调试工具

  ```javascript
  import VConsole from 'vconsole';
  new VConsole();
  ```

- **Eruda** - 另一个调试工具

### 真机测试

- **Safari 开发者工具**（iOS）
- **Chrome Remote Debugging**（Android）
- **微信开发者工具**（微信浏览器）

---

## 💡 最佳实践

### ✅ 以 375px 为基准

- 设计稿通常为 750px（2 倍图）
- 开发时除以 2，或使用自动转换工具

### ✅ 最小点击区域 44px

- iOS 推荐最小点击区域：44x44px
- Android 推荐：48x48px

### ✅ 真机测试是必须的

- 不要只在浏览器模拟器中测试
- 必须在真实设备上验证

### ✅ 性能优化

- 移动端性能要求更高
- 图片懒加载
- 虚拟列表（长列表）
- 按需加载组件

---

**文档创建：** 2025-12-07
**维护者：** ****\_\_\_****
