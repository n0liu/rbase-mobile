# Next.js + Ant Design Mobile 移动端项目创建规划

## 📋 项目信息

- **项目名称**: rbase-mobile
- **项目路径**: /Users/n0liu/Desktop/rbase-mobile（当前目录）
- **技术栈**: React + Next.js 15 + TypeScript + Ant Design Mobile
- **路由模式**: App Router
- **目标**: 创建企业级移动端 H5 应用基础架构

**重要**: 直接在当前目录初始化项目，不创建子目录

---

## 🎯 核心依赖版本规划

### 主框架
```json
{
  "next": "^15.1.0",
  "react": "^19.0.0",
  "react-dom": "^19.0.0",
  "typescript": "^5.7.2"
}
```

**说明**: Next.js 15 是当前最新稳定版，与 React 19 配合使用，性能和开发体验都更好。

### UI 组件库
```json
{
  "antd-mobile": "^5.37.1"
}
```

**说明**: Ant Design Mobile 5.x 版本，基于 React 18+，主题定制能力强。

### 移动端适配
```json
{
  "postcss-px-to-viewport": "^1.1.1"
}
```

**说明**: 自动将 px 转换为 vw，基于 375px 设计稿，无需手动计算。

### 状态管理
```json
{
  "zustand": "^5.0.2"
}
```

**说明**: 选择 Zustand 而非 Redux，因为：
- 更轻量（~1KB）
- API 更简单
- 与 React 19 兼容性好
- 支持 TypeScript
- 适合移动端项目

### HTTP 请求
```json
{
  "axios": "^1.7.9"
}
```

**说明**: 企业级 HTTP 客户端，支持拦截器、请求取消等高级特性。

### 开发工具
```json
{
  "eslint": "^9",
  "eslint-config-next": "^15.1.0",
  "vconsole": "^3.15.1"
}
```

**说明**: vConsole 用于移动端真机调试，开发阶段必备。

---

## 📁 项目目录结构

**当前目录**: /Users/n0liu/Desktop/rbase-mobile

```
. (当前目录)
├── app/                          # Next.js 15 App Router
│   ├── layout.tsx               # 根布局（配置主题、字体等）
│   ├── page.tsx                 # 首页
│   ├── globals.css              # 全局样式
│   ├── demo/                    # 示例页面
│   │   └── page.tsx            # 组件展示页
│   └── api/                     # API 路由（可选）
│       └── hello/
│           └── route.ts
│
├── src/
│   ├── components/              # 公共组件
│   │   ├── CustomNavBar/       # 自定义导航栏
│   │   │   ├── index.tsx
│   │   │   └── index.module.css
│   │   ├── CustomCard/         # 自定义卡片
│   │   │   ├── index.tsx
│   │   │   └── index.module.css
│   │   └── Layout/             # 布局组件
│   │       ├── index.tsx
│   │       └── index.module.css
│   │
│   ├── theme/                   # 主题配置
│   │   ├── index.ts            # Ant Design Mobile 主题变量
│   │   └── themes.ts           # 多主题方案（可选）
│   │
│   ├── styles/                  # 样式文件
│   │   ├── variables.css       # CSS 变量
│   │   └── custom.css          # 自定义样式覆盖
│   │
│   ├── store/                   # Zustand 状态管理
│   │   ├── index.ts            # Store 入口
│   │   ├── userStore.ts        # 用户状态
│   │   └── appStore.ts         # 应用全局状态
│   │
│   ├── services/                # API 服务
│   │   ├── request.ts          # Axios 封装（拦截器、错误处理）
│   │   ├── api/                # API 接口定义
│   │   │   ├── user.ts
│   │   │   └── common.ts
│   │   └── types/              # API 类型定义
│   │       └── index.ts
│   │
│   ├── utils/                   # 工具函数
│   │   ├── flexible.ts         # rem 适配（可选）
│   │   ├── storage.ts          # 本地存储封装
│   │   └── validator.ts        # 表单验证
│   │
│   ├── hooks/                   # 自定义 Hooks
│   │   ├── useRequest.ts       # 请求 Hook
│   │   └── useDebounce.ts      # 防抖 Hook
│   │
│   └── types/                   # 全局类型定义
│       └── index.ts
│
├── public/                      # 静态资源
│   ├── favicon.ico
│   └── images/
│
├── .vscode/                     # VSCode 配置
│   └── settings.json
│
├── docs/                        # 项目文档
│   ├── 移动端H5组件库定制方案.md  # 原始设计方案
│   └── 项目创建规划.md          # 本规划文档
│
├── next.config.ts               # Next.js 配置
├── postcss.config.mjs           # PostCSS 配置（px 转 vw）
├── tsconfig.json                # TypeScript 配置
├── .eslintrc.json               # ESLint 配置
├── .gitignore
├── package.json
└── README.md
```

---

## 🔧 关键配置文件

### 1. next.config.ts
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // 移动端优化
  compress: true,

  // 图片优化
  images: {
    formats: ['image/avif', 'image/webp'],
  },

  // 开发时关闭严格模式（避免 Ant Design Mobile 警告）
  reactStrictMode: false,
};

export default nextConfig;
```

### 2. postcss.config.mjs
```javascript
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    'postcss-px-to-viewport': {
      viewportWidth: 375,        // 设计稿宽度
      unitPrecision: 5,          // 转换精度
      viewportUnit: 'vw',        // 转换单位
      selectorBlackList: ['.ignore', '.hairlines'],  // 不转换的类名
      minPixelValue: 1,          // 最小转换值
      mediaQuery: false,
    },
  },
};

export default config;
```

### 3. tsconfig.json (关键配置)
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### 4. .vscode/settings.json
```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "typescript.tsdk": "node_modules/typescript/lib"
}
```

---

## 🎨 核心文件内容规划

### 1. 主题配置 (src/theme/index.ts)
```typescript
export const mobileTheme = {
  // 颜色系统
  '--adm-color-primary': '#1677FF',
  '--adm-color-success': '#52c41a',
  '--adm-color-warning': '#faad14',
  '--adm-color-danger': '#ff3141',

  '--adm-color-text': '#333333',
  '--adm-color-text-secondary': '#666666',
  '--adm-color-weak': '#999999',

  '--adm-color-background': '#ffffff',
  '--adm-color-border': '#eeeeee',

  // 字体系统
  '--adm-font-size-main': '14px',
  '--adm-font-size-3': '12px',
  '--adm-font-size-7': '16px',
  '--adm-font-size-8': '18px',

  // 圆角
  '--adm-radius-s': '4px',
  '--adm-radius-m': '8px',
  '--adm-radius-l': '12px',

  // 组件配置
  '--adm-button-border-radius': '8px',
  '--adm-nav-bar-height': '44px',
  '--adm-list-item-padding-left': '16px',
};
```

### 2. 根布局 (app/layout.tsx)
```typescript
import { ConfigProvider } from 'antd-mobile';
import { mobileTheme } from '@/theme';
import './globals.css';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="zh-CN">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
      </head>
      <body>
        <ConfigProvider theme={mobileTheme}>
          {children}
        </ConfigProvider>
      </body>
    </html>
  );
}
```

### 3. Axios 封装 (src/services/request.ts)
```typescript
import axios from 'axios';

const request = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_BASE_URL || '/api',
  timeout: 10000,
});

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    // 添加 token
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器
request.interceptors.response.use(
  (response) => response.data,
  (error) => {
    // 统一错误处理
    console.error('API Error:', error);
    return Promise.reject(error);
  }
);

export default request;
```

### 4. Zustand Store 示例 (src/store/userStore.ts)
```typescript
import { create } from 'zustand';

interface UserState {
  user: any | null;
  token: string | null;
  setUser: (user: any) => void;
  setToken: (token: string) => void;
  logout: () => void;
}

export const useUserStore = create<UserState>((set) => ({
  user: null,
  token: null,
  setUser: (user) => set({ user }),
  setToken: (token) => set({ token }),
  logout: () => set({ user: null, token: null }),
}));
```

---

## 📦 安装步骤规划

### 步骤 1: 在当前目录初始化 Next.js 项目
```bash
# 使用 . 表示在当前目录初始化
npx create-next-app@latest . --typescript --eslint --app --no-tailwind --src-dir --import-alias "@/*"
```

**重要**：
- 使用 `.` 而不是项目名，直接在当前目录初始化
- 遇到"是否使用 React Compiler"时选择 **No**
- 当前目录是：/Users/n0liu/Desktop/rbase-mobile

创建时选择：
- ✅ TypeScript → **YES**
- ✅ ESLint → **YES**
- ✅ App Router → **YES**
- ✅ Tailwind CSS → **NO** (使用 Ant Design Mobile 的样式)
- ✅ src/ directory → **YES**
- ✅ import alias (@/*) → **YES**
- ✅ React Compiler → **NO**

### 步骤 2: 安装核心依赖
```bash
# UI 组件库
npm install antd-mobile

# 状态管理
npm install zustand

# HTTP 请求
npm install axios

# 移动端适配
npm install -D postcss-px-to-viewport

# 移动端调试
npm install vconsole
```

### 步骤 3: 创建目录结构
```bash
# 在 src 下创建目录
mkdir -p src/{components,theme,styles,store,services,utils,hooks,types}
mkdir -p src/services/{api,types}
mkdir -p src/components/{CustomNavBar,CustomCard,Layout}

# 创建 docs 目录
mkdir -p docs

# 创建 .vscode 目录
mkdir -p .vscode
```

### 步骤 4: 整理项目文档
```bash
# 将原始设计方案移动到 docs 目录
mv 移动端H5组件库定制方案.md docs/

# 将规划文档复制到 docs 目录
cp /Users/n0liu/.claude/plans/dreamy-growing-dragonfly.md docs/项目创建规划.md
```

**说明**: 将文档统一放在 docs/ 目录下，保持项目根目录整洁。

### 步骤 5: 创建配置文件
- postcss.config.mjs (移动端适配)
- src/theme/index.ts (主题配置)
- src/services/request.ts (Axios 封装)
- src/store/userStore.ts (状态管理示例)
- .vscode/settings.json (编辑器配置)

### 步骤 6: 修改 Next.js 配置
- 更新 next.config.ts
- 更新 app/layout.tsx (添加 ConfigProvider)

### 步骤 7: 创建示例页面
- app/demo/page.tsx (组件展示页)
- 演示 Button、List、Card、NavBar 等组件

### 步骤 8: 创建自定义组件
- src/components/CustomNavBar (统一的顶部导航)
- src/components/CustomCard (统一的卡片样式)
- src/components/Layout (页面布局)

---

## 🎯 开发时注意事项

### 1. 移动端适配
- 所有 px 会自动转为 vw
- 如需保持 px，添加 `/* px */` 注释
- 设计稿基准：375px

### 2. vConsole 调试
在 app/layout.tsx 中添加（仅开发环境）：
```typescript
'use client';
import { useEffect } from 'react';

if (process.env.NODE_ENV === 'development') {
  useEffect(() => {
    import('vconsole').then(({ default: VConsole }) => {
      new VConsole();
    });
  }, []);
}
```

### 3. 环境变量
创建 `.env.local`:
```
NEXT_PUBLIC_API_BASE_URL=https://api.example.com
```

### 4. 移动端测试
- Chrome DevTools 移动端模拟
- 真机测试（扫码访问）
- 不同浏览器测试（Safari、微信）

---

## 🚀 启动命令

```bash
# 开发
npm run dev

# 构建
npm run build

# 生产环境运行
npm run start

# 代码检查
npm run lint
```

访问: http://localhost:3000

---

## 📱 移动端最佳实践集成

### 1. 安全区域适配 (globals.css)
```css
body {
  padding-top: env(safe-area-inset-top);
  padding-bottom: env(safe-area-inset-bottom);
}
```

### 2. 1px 边框方案
Ant Design Mobile 已内置，无需额外处理。

### 3. 点击延迟
使用 CSS:
```css
* {
  touch-action: manipulation;
}
```

### 4. 滚动优化
```css
.scroll-container {
  -webkit-overflow-scrolling: touch;
}
```

---

## ✅ 验收标准

项目创建完成后应该能够：

1. ✅ 运行 `npm run dev`，项目正常启动
2. ✅ 访问首页，看到 Ant Design Mobile 组件
3. ✅ 在移动端模式下，样式正常显示
4. ✅ 打开 vConsole，能看到调试面板
5. ✅ 主题变量生效（按钮颜色、字体等）
6. ✅ TypeScript 无报错
7. ✅ 能够正常导入 @/xxx 路径
8. ✅ Zustand store 正常工作
9. ✅ Axios 请求封装可用

---

## 📝 推荐的下一步

项目创建完成后，建议：

1. 根据《移动端H5组件库定制方案.md》梳理设计规范
2. 调整 src/theme/index.ts 中的颜色、字体等变量
3. 创建常用的业务组件
4. 配置真机调试环境
5. 接入实际的后端 API

---

**规划完成时间**: 2025-12-08
**预计执行时间**: 30-40 分钟
